import { BackgroundPaths } from '@/components/ui/background-paths'

export function DemoBackgroundPaths() {
  return (
    <div className="relative h-[28rem] w-full overflow-hidden rounded-2xl bg-[var(--navy)]">
      <BackgroundPaths />
    </div>
  )
}
